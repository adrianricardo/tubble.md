# Source — 2026-07-09: Adrian's triage answers, first hubble-init dry run

Raw capture (append-only) of Adrian's AskUserQuestion answers during the first
`/hubble-init brain/` dry run (full run record:
`/specs/hubble-init/runs/2026-07-09-brain-first-dry-run.md`).

- On RESOLVER/BRAINKEEPER: keep in git — "i think makes sense, but cant we just have a
single file in git that also governs the cloud files? our decision here should become
the default behavior of the skill."
- On decision-log: "this should become a default for the generalized skill"; confirmed
in follow-up: **split the file** (strategy → cloud, engineering → git).
- On roadmap: **split the file**.
- On activity-log: **move with corpus**.
- Did not challenge any uncontested call (README keep; current-vision, open-questions,
session sources, pending-extraction move).

Notable: twice he framed answers as *defaults for the generalized skill*, not one-off
calls for this repo — he's designing the product's default behavior through the dogfood
answers, which is exactly the "modify as we go" loop working.
