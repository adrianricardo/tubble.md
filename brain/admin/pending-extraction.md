# Pending Extraction — the vision in Adrian's head

**Status: OPEN** (declared 2026-07-09).

Adrian: "there is a LOT more in my head, but I'm not ready to extract it yet."

What this means for agents:

- `synthesized/current-vision.md` is a partial approximation. Do not fill gaps with
  invention; file open questions instead.
- Large app-UX rework ("make the app match my vision and UX") should wait for, or be
  explicitly scoped around, the extraction.
- When Adrian is ready: run an **InterviewMe-style session** (his global CLAUDE.md
  defines the protocol — AskUserQuestion tool, most impactful questions first, no
  obvious questions). Output: revised `current-vision.md` + decision-log entries +
  roadmap re-derivation. Consider the `/office-hours` or `/mockup` skills as follow-ups
  for UX specifics.
- Close this file (mark DONE with date + source link) when the extraction session has
  been captured under `sources/`.
