# Pending Extraction — the vision in Adrian's head

**Status: OPEN** (declared 2026-07-09).

Adrian: "there is a LOT more in my head, but I'm not ready to extract it yet."

What this means for agents:

- `synthesized/current-vision.md` is a partial approximation. Do not fill gaps with
invention; file open questions instead.
- Large app-UX rework ("make the app match my vision and UX") should wait for, or be
explicitly scoped around, the extraction.
- When Adrian is ready: run an **InterviewMe-style session** (his global CLAUDE.md
defines the protocol — AskUserQuestion tool, most impactful questions first, no
obvious questions). Output: revised `synthesized/current-vision.md` + decision-log
entries + roadmap re-derivation. Consider the `/office-hours` or `/mockup` skills as
follow-ups for UX specifics.
- Close this file (mark DONE with date + source link) when the extraction session has
been captured under `sources/`.

## Explicitly extracted slice (2026-07-11)

Desktop navigation and watched local-projection behavior are no longer unknown. Adrian
resolved one cloud tree, no standalone local-authority product mode, quit-time edit
preservation, contextual local availability, targeted access-impact confirmation, and
Trash/recovery behavior. Source:
`../sources/2026-07-11-desktop-navigation-ia.md`; normative contract:
`/specs/desktop-cloud-workspace/PRODUCT.md`. This does not close the broader extraction.

## Explicitly extracted correction (2026-07-15)

Git is the default authority for repository content. A folder moves to Hubble Cloud
only for realtime collaboration or repository-independent access/privacy, and it can
move back to Git. This supersedes the earlier universal-cloud assumption without
discarding the cloud projection safety work. Source:
`../sources/2026-07-15-git-default-folder-authority.md`; normative contract:
`/specs/folder-authority-mobility/PRODUCT.md`.
