# Upstream intake — 2026-07-14

## Status

Planning pass complete. A normal fetched audit found no commits newer than the existing
watermark. Adrian approved the correctness candidates for later reimplementation and
selected upstream-style roomier editor spacing plus cloud-compatible sidebar creation
controls and row menus. This run wrote the implementation plan; it did not port product
code or resolve the retained queue entries as landed.

## Captured starting state

- Target branch/ref: `v1-release` / `refs/heads/v1-release`
- Target HEAD: `a544bc1b43b23202596de36a8ebeaa0de7428b3c`
- Checked-out branch/HEAD: `v1-release` / `a544bc1b43b23202596de36a8ebeaa0de7428b3c`
- Starting worktree: clean
- Intake branch: `codex/upstream-intake-2026-07-14`
- Intake worktree: `/Users/adriantavares/Code/hubble.md-upstream-intake-2026-07-14`
- Upstream remote/ref: `upstream` / `refs/remotes/upstream/main`
- Upstream HEAD: `72c9e808cb32bb9c451b6694ced4a8ea6775906f`
- Screened-through watermark before run: `72c9e808cb32bb9c451b6694ced4a8ea6775906f`
- Newly seen range: empty (`screenedThrough == upstream HEAD`)
- Merge base: `2a9983b2e94bf8d1596285925dfcee7429f77df7`
- Divergence: 156 fork-only / 243 upstream-only commits
- Shared changed paths: 36
- Synthetic merge preview: unclean, 27 conflict events affecting 28 reported paths
- Automatic landing eligibility at capture: yes, subject to every final gate

## Candidate review

Behavior-level inspection compared all retained candidates with the fork's
cloud-authority, projection, serialization, and current-context contracts.

Selected for later reimplementation, while remaining queued until verified and landed:

- UTF-8 save bytes and self-save watcher race (`c99e80df`)
- editor/sync edge hardening split into independent behaviors (`60905b68`)
- mixed image/text Markdown preservation (`fed659c5`)
- rich-text link clipboard serialization adapted to fork link metadata (`66a17874`)
- macOS native text menu and caret spellcheck (`9732d4b3`, `7fa5dffe`)
- asset HTTP failure handling with fork-safe retry/status semantics (`df7560f8`)
- roomier editor block spacing (`f6c44a24`)

The serializer consolidation (`7d290971`) remains likely `superseded`; its focused and
seeded property tests should be run against the fork before that final disposition.

Global search, back/forward navigation, source mode, system-follow dark mode, and editor
Find were not ranked in the product interview and retain `defer-product`.

Adrian also selected the interaction value of upstream's newer sidebar create controls
and per-row menus. The historical upstream local-sidebar implementation remains
incompatible, but its interaction pattern is now selected for `reimplement` on the
cloud-ID tree. Relevant upstream references are `416564a`, `3023212`, `9d5f42c`, and
`e38be95`; direct filesystem authority, direct deletes, and path identity remain skipped.

Implementation plan:
`specs/upstream-selected-improvements/TECH.md`.

## Verification and landing

- Product-code verification: not applicable; no product code changed
- Planning-document checks: `git diff --check` passed; all six required TECH sections
  are present. Repository Biome intentionally ignores Markdown, so it processed zero
  files and is not claimed as Markdown validation.
- Simplify: not applicable to product code
- Review readiness: not applicable; implementation has not started
- Watermark after run: unchanged at `72c9e808cb32bb9c451b6694ced4a8ea6775906f`
- Candidate queue: unchanged; selected items stay queued until a later implementation
  run records verified final dispositions
- Landing outcome: default-mode guarded fast-forward selected. This record is finalized
  before the ref update; landing occurs only if the original `v1-release` worktree and
  captured `a544bc1` target remain clean and unchanged. The final task report records
  the resulting HEAD or the exact failed gate.
